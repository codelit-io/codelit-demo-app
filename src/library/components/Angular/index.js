import React from "react";
import PageHeader from "../../../components/shared/PageHeader";

const Angular = ({ topic , course}) => {
	return <PageHeader title={topic} course={course}></PageHeader>;
};

export default Angular;
