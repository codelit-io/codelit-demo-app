const styles = theme => ({
	editor: {
		background: "#fff",
		borderRadius: "8px",
		boxShadow:
			bigShadow,
		width: "100%"
	},
	preview: {
		// background: "#fff",
		borderRadius: "8px",
		// padding: "10px",
		// boxShadow:bigShadow,
		// width: "100%"
	}
});

export default styles;
