const styles = theme => ({
  avatar: {
    width: theme.spacing(4),
    height: theme.spacing(4)
  },
  adminAvatar: {
    border: `2px solid ${theme.yellow.light}`,
    overflow: "visible"
  }
});

export default styles;
