const styles = theme => ({
  text: {
    color: theme.grey?.medium,
    textDecoration: 'none',
    marginBottom: theme.space?.md,
    width: '100%',
    border: 'none',
    outline: 'none',
  },
});

export default styles;
