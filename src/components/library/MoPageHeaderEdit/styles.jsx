const styles = theme => ({
  text: {
    color: theme.grey?.light,
    backgroundColor: "transparent",
    textDecoration: "none",
    marginBottom: theme.space?.md,
    width: "100%",
    border: "none",
    outline: "none"
  }
});

export default styles;
