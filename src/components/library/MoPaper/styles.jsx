const styles = (theme) => ({
  card: {
    ...theme.card,
    paddingTop: theme.spacing(4),
    paddingBottom: theme.spacing(4),
    width: "100%",
  },
  disableCard: {
    opacity: "0.5",
  },
  link: {
    width: "100%",
  },
});

export default styles;
