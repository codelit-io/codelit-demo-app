const styles = (theme) => ({
  title: {
    color: theme.grey?.medium,
    textDecoration: "none",
    marginBottom: theme.padding?.md,
    width: "100%",
    border: "none",
    outline: "none",
  },
});

export default styles;
