const styles = (theme) => ({
  container: {
    paddingTop: theme.space.xl
  },
  footerText: {
    textAlign: "right"
  },
  footerButton: {
    textTransform: "initial"
  }
});

export default styles;
