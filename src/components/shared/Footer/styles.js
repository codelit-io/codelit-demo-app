const styles = theme => ({
  container: {
    paddingTop: theme.space.xl
  },
  footerButton: {
    textTransform: "initial"
  },
  footer: {
    zIndex: "10",
    position: "fixed",
    bottom: 0,
    right: 0,
    width: "100%"
  }
});

export default styles;
