const styles = theme => ({
  link: {
    backgroundColor: "#41D3BD",
    textDecoration: "none",
    padding: theme.spacing(2),
    color: "#FFFFF2",
    fontSize: "20px",
    boxShadow: theme.bigShadow,
    borderRadius: "3px"
  }
});

export default styles;
