const styles = theme => ({
	button: {
		padding: theme.spacing(2),
		color: "#FFFFF2",
		fontSize: "20px",
		boxShadow: bigShadow,
		backgroundColor: "#41D3BD",
		borderRadius: "3px",
		"&:hover": {
			backgroundColor: "#41D3BD",
		}
	}
});

export default styles;