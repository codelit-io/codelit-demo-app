const styles = (theme) => ({
  root: {
    flexGrow: 1,
    paddingBottom: theme.space?.lg,
  },
  appBar: {
    boxShadow: "none",
  },
  leftSide: {
    display: "flex",
    alignItems: "center",
  },
});

export default styles;
