const styles = theme => ({
  container: {
    paddingTop: theme.padding.xl
  },
  footerText: {
    textAlign: "right"
  },
  footerButton: {
    textTransform: "initial"
  }
});

export default styles;
